{
  "name": "weeber-server",
  "version": "0.1.0",
  "description": "Weeber registry, auth, and billing API. Designed for 1M users on a 1GB VPS.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "dev": "node --watch --env-file=.env src/server.js",
    "migrate": "node src/db/migrate.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@fastify/helmet": "^12.0.0",
    "@fastify/rate-limit": "^10.0.0",
    "@fastify/websocket": "^11.0.0",
    "argon2": "^0.41.0",
    "fastify": "^5.0.0",
    "jose": "^5.9.0",
    "stripe": "^17.0.0"
  }
}
